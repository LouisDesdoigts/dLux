import dLux
import jax.numpy as numpy 

amplitude = numpy.ones((128, 128))
phase = numpy.zeros((128, 128))
wavelength = 550e-09
offset = [0., 0.]
position = 0.
beam_radius = 1
phase_radius = 1 # This quantity is unused and can be removed

